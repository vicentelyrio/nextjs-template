import { createTheme } from '@mui/material/styles'

const theme = createTheme({
  palette: {
    primary: {
      main: '#27a265',
    },
    secondary: {
      main: '#ec4c4d',
    },
    background: {
      default: '#2b2b2b',
      paper: '#414141',
    },
  },
})

export default theme
